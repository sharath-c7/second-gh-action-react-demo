name: Output Information
on: workflow_dispatch
jobs:
    info:
        runs-on: ubuntu-latest
        steps:
            - name: Output Information
              run: |
                echo "${{ toJSON(github) }}"